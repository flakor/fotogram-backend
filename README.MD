comandos tsc del inicializacion del proyecto

npm init

tsc --init

tsc -w  modo escucha


tsconfig.json
 "target": "es6",  
 "outDir": "dist/",   



 npm install -g nodemon


 nodemon dist/


 npm install express body-parser cors mongoose express-fileupload jsonwebtoken bcrypt